<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ("partials/head.ejs") %>
</head>
<body>
    <%- include ("partials/header_logged.ejs") %>
    <main>
        <div class="product-container">
            <div class="product-main">
                <div class="product-img">
                    <img src="/products/<%= itens.image %>" alt="Foto do Produto" class="product-img-img" id="img-info">
                </div>
                <p id="sec-info" hidden><%= itens.section %></p>
                <div>
                    <h1 id="name-info"><%= itens.name %></h1>
                    <p id="preco-info"><%= itens.price %></p>

                    <button class="product-info-button" onclick="addLittleCar()">Adicionar ao carrinho</button>
                </div>
            </div>
        </div>
    </main>
</body>
</html>